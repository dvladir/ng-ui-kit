import { Masks2ListItemsPipe } from './masks2-list-items.pipe';

describe('Masks2ListItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new Masks2ListItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
