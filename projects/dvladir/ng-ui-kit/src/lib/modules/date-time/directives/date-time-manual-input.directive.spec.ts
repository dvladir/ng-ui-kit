/*
import { DateTimeManualInputDirective } from './date-time-manual-input.directive';

describe('DateTimeManualInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeManualInputDirective();
    expect(directive).toBeTruthy();
  });
});
*/
