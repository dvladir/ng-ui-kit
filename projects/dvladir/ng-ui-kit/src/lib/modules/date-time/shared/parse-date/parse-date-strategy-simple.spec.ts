import { ParseDateStrategySimple } from './parse-date-strategy-simple';

describe('ParseDateStrategySimple', () => {
  it('should create an instance', () => {
    expect(new ParseDateStrategySimple()).toBeTruthy();
  });
});
