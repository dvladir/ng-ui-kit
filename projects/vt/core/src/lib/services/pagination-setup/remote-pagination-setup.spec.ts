import { RemotePaginationSetup } from './remote-pagination-setup';

describe('RemotePaginationSetup', () => {
  it('should create an instance', () => {
    expect(new RemotePaginationSetup()).toBeTruthy();
  });
});
