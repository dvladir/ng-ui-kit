import { LocalPaginationSetup } from './local-pagination-setup';
import {of} from 'rxjs';

describe('LocalPaginationSetup', () => {
  it('should create an instance', () => {
    //expect(new LocalPaginationSetup(of([]))).toBeTruthy();
  });
});
