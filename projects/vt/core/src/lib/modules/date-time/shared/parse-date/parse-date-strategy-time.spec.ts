import { ParseDateStrategyTime } from './parse-date-strategy-time';

describe('ParseDateStrategyTime', () => {
  it('should create an instance', () => {
    expect(new ParseDateStrategyTime()).toBeTruthy();
  });
});
