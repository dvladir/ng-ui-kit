/*
import { FixedListProvider } from './fixed-list-provider';

describe('FixedListProvider', () => {
  it('should create an instance', () => {
    expect(new FixedListProvider()).toBeTruthy();
  });
});
*/
